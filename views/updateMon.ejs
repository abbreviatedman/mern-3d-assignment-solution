<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head') %>
<body>
    <!-- We'll assume that we will get the current pokemon as an object called pokemon. -->
    <!-- we'll add res.render('updateMon', pokemon) -->
    <h1>Editing: <%= pokemon.Name %></h1>
    <form action="/api/pokemon/<%= pokemon.Name %>?_method=PUT" method="POST">
        <label for="PokedexNo">PokedexNo:</label>
        <input type="text" id="PokedexNo" name="PokedexNo" value="<%= pokemon.PokedexNo %>">
        <br><br>
        
        <label for="Name">Name:</label>
        <input type="text" id="Name" name="Name" value="<%= pokemon.Name %>">
        <br><br>
        
        <label for="Type">Type:</label>
        <input type="text" id="Type" name="Type" value="<%= pokemon.Type %>">
        <br><br>
        
        <label for="Moves">Moves:</label>
        <input type="text" id="Moves" name="Moves" value="<%= pokemon.Moves.join(", ") %>">
        <br><br>
        
        <input type="submit" name="_method" value="SAVE CHANGES">
    </form>
</body>
</html>